<?php

namespace App\Services\Store;

use Illuminate\Support\Facades\Auth;


class StoreActionService
{
	/*
    public function favorite($productId)
    {
        if (!Auth::check()) {
            return [
                'ok' => false,
                'reason' => 'auth_required',
            ];
        }

        // call real favorite logic (domain layer)
        app(FavoriteService::class)->toggle($productId);

        return ['ok' => true];
    }

*/



}